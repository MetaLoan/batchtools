# Server
PORT=3000
HOST=0.0.0.0
DATA_DIR=./data

# Public-facing host (used to build upload signed URLs that DashScope can fetch)
# In dev, this should be your tunneled URL if DashScope must reach you; in production set to your fly.dev URL.
PUBLIC_HOST=http://localhost:3000

# Auth
APP_PASSWORD=changeme
SESSION_TTL_DAYS=30

# Crypto master key for encrypting account API keys & signing upload URLs
MASTER_KEY=replace-with-a-long-random-string

# Upload limits
UPLOAD_MAX_BYTES=26214400
UPLOAD_TTL_HOURS=24
